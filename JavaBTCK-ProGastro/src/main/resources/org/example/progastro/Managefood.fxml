<?xml version="1.0" encoding="UTF-8"?>

<?import java.lang.String?>
<?import javafx.collections.FXCollections?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.ComboBox?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.TableColumn?>
<?import javafx.scene.control.TableView?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.image.ImageView?>
<?import javafx.scene.layout.AnchorPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.VBox?>

<AnchorPane fx:id="root" prefHeight="675.0" prefWidth="1225.0" xmlns="http://javafx.com/javafx/23.0.1" xmlns:fx="http://javafx.com/fxml/1" fx:controller="controller.ManageFoodController" stylesheets="@ManageFood.css">

    <!-- === LEFT FORM PANEL === -->
    <VBox layoutX="89.0" layoutY="14.0" spacing="15">

        <Label style="-fx-font-size: 22px; -fx-font-weight: bold;" text="Quản lý món ăn" />

        <Button fx:id="addNewButton" onAction="#handleAddNewFood" style=" -fx-text-fill: white;" text="➕ Thêm món mới" />

        <!-- NAME -->
        <Label text="Tên món:" />
        <TextField fx:id="nameField" prefWidth="250" promptText="Nhập tên món ăn" />

        <!-- DESCRIPTION -->
        <Label text="Mô tả:" />
        <TextField fx:id="descriptionField" prefWidth="250" promptText="Nhập mô tả món ăn" />

        <Label text="Loại món:" />
        <ComboBox fx:id="categoryCombo" prefWidth="200">
            <items>
                <FXCollections fx:factory="observableArrayList">
                    <String fx:value="Món Bò" />
                    <String fx:value="Món Gà" />
                    <String fx:value="Hải Sản" />
                    <String fx:value="Pizza" />
                    <String fx:value="Mì / Bún / Phở" />
                    <String fx:value="Salad" />
                    <String fx:value="Bánh - Bakery" />
                    <String fx:value="Đồ Uống" />
                    <String fx:value="Khác" />

                </FXCollections>
            </items>
        </ComboBox>


        <!-- PRICE -->
        <Label text="Giá:" />
        <TextField fx:id="priceField" prefWidth="150" promptText="Nhập giá" />

        <!-- IMAGE -->
        <Label text="Ảnh món:" />
        <ImageView fx:id="foodImageView" fitHeight="120" fitWidth="160" preserveRatio="true" style="-fx-border-color: gray; -fx-background-color: white;" />

        <HBox spacing="10">
            <Button fx:id="chooseImageButton" onAction="#handleChooseImage" text="Chọn ảnh" />
            <Button fx:id="deleteImageButton" onAction="#handleDeleteImage" text="Xóa ảnh" />
        </HBox>

        <!-- SAVE + DELETE -->
        <HBox spacing="10">
            <Button fx:id="saveButton" onAction="#handleSave" prefWidth="120" text="💾 Lưu" />
            <Button fx:id="deleteButton" onAction="#handleDeleteFood" prefWidth="120" text="🗑 Xóa" />
        </HBox>

    </VBox>

    <!-- === RIGHT TABLE PANEL === -->
    <TableView fx:id="foodTable" layoutX="508.0" layoutY="30.0" prefHeight="550" prefWidth="631.0">
        <columns>
            <TableColumn fx:id="nameColumn" prefWidth="160" text="Tên món" />
            <TableColumn fx:id="descriptionColumn" prefWidth="200" text="Mô tả" />
            <TableColumn fx:id="priceColumn" prefWidth="120" text="Giá" />
            <TableColumn fx:id="categoryColumn" prefWidth="150" text="Loại món" />
        </columns>
    </TableView>

</AnchorPane>
