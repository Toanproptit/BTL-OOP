<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.image.ImageView?>

<?import javafx.collections.FXCollections?>
<?import java.lang.String?>
<AnchorPane fx:id="root" prefWidth="900" prefHeight="600"
            xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="controller.ManageFoodController">

    <!-- === LEFT FORM PANEL === -->
    <VBox spacing="15" layoutX="20" layoutY="20">

        <Label text="Quản lý món ăn" style="-fx-font-size: 22px; -fx-font-weight: bold;"/>

        <Button fx:id="addNewButton" text="➕ Thêm món mới"
                style="-fx-background-color: #4CAF50; -fx-text-fill: white;"
                onAction="#handleAddNewFood"/>

        <!-- NAME -->
        <Label text="Tên món:"/>
        <TextField fx:id="nameField" promptText="Nhập tên món ăn" prefWidth="250"/>

        <!-- DESCRIPTION -->
        <Label text="Mô tả:"/>
        <TextField fx:id="descriptionField" promptText="Nhập mô tả món ăn" prefWidth="250"/>

        <Label text="Loại món:" />
        <ComboBox fx:id="categoryCombo" prefWidth="200">
            <items>
                <FXCollections fx:factory="observableArrayList">
                    <String fx:value="Món Bò" />
                    <String fx:value="Món Gà" />
                    <String fx:value="Hải Sản" />
                    <String fx:value="Pizza" />
                    <String fx:value="Mì / Bún / Phở" />
                    <String fx:value="Salad" />
                    <String fx:value="Bánh - Bakery" />
                    <String fx:value="Đồ Uống" />
                    <String fx:value="Khác" />

                </FXCollections>
            </items>
        </ComboBox>


        <!-- PRICE -->
        <Label text="Giá:"/>
        <TextField fx:id="priceField" promptText="Nhập giá" prefWidth="150"/>

        <!-- IMAGE -->
        <Label text="Ảnh món:"/>
        <ImageView fx:id="foodImageView" fitHeight="120" fitWidth="160" preserveRatio="true"
                   style="-fx-border-color: gray; -fx-background-color: white;"/>

        <HBox spacing="10">
            <Button fx:id="chooseImageButton" text="Chọn ảnh" onAction="#handleChooseImage"/>
            <Button fx:id="deleteImageButton" text="Xóa ảnh" onAction="#handleDeleteImage"/>
        </HBox>

        <!-- SAVE + DELETE -->
        <HBox spacing="10">
            <Button fx:id="saveButton" text="💾 Lưu" prefWidth="120" onAction="#handleSave"/>
            <Button fx:id="deleteButton" text="🗑 Xóa" prefWidth="120" onAction="#handleDeleteFood"/>
        </HBox>

    </VBox>

    <!-- === RIGHT TABLE PANEL === -->
    <TableView fx:id="foodTable" layoutX="350" layoutY="20" prefWidth="530" prefHeight="550">
        <columns>
            <TableColumn fx:id="nameColumn" text="Tên món" prefWidth="160"/>
            <TableColumn fx:id="descriptionColumn" text="Mô tả" prefWidth="200"/>
            <TableColumn fx:id="priceColumn" text="Giá" prefWidth="120"/>
        </columns>
    </TableView>

</AnchorPane>
